# MDX Showcase

This page demonstrates the various capabilities of MDX files, which combine Markdown with JSX.

## Basic Markdown Features

### Text Formatting
You can write text with **bold**, *italic*, or ***both***. 
You can also use ~~strikethrough~~ and `inline code`.

### Lists
Unordered list:
- Item 1
- Item 2
  - Nested item
  - Another nested item
- Item 3

Numbered list:
1. First item
2. Second item
3. Third item

### Blockquotes
> This is a blockquote
> It can span multiple lines
>> And can be nested

### Callouts
> **Warning**: This is a warning callout that you should pay attention to.
{' '}
{/* Adding space for better readability */}

> **Note**: Here's an important note you should know about.
{' '}

> **Tip**: Here's a helpful tip to improve your workflow.
{' '}

> **Info**: Here's some useful information.

### Code Blocks
```python
def hello_world():
    print("Hello, World!")
```

### Tables
| Feature | Supported | Notes |
|---------|-----------|-------|
| Tables | ✅ | With alignment |
| Lists | ✅ | Nested too |
| Code | ✅ | With syntax highlighting |

## React Components

import Counters from '../../components/counters'

### Interactive Counter Component
Below is a React component embedded in our MDX:

<Counters />

## Special Features

### Custom Links
Here's a link to [another page](/advanced/satori).

### Task Lists
- [x] Write documentation
- [ ] Add more examples
- [ ] Review content

### Technical Terms with Tooltips
import TextTooltip from '../../components/TextTooltip'

The <TextTooltip text="A large language model is a type of artificial intelligence model trained to understand and generate human-like text">LLM</TextTooltip> has revolutionized natural language processing. Another example is <TextTooltip text="Transformers are a type of neural network architecture that uses self-attention mechanisms">transformer architecture</TextTooltip> which has become fundamental to modern AI.

### Footnotes
Here's a sentence with a footnote[^1].

[^1]: This is the footnote content.

## Advanced MDX Features

### Using HTML with Markdown
<div style={{ padding: '1rem', backgroundColor: '#f0f0f0', borderRadius: '4px' }}>
  This is a styled div containing **markdown** content!
</div>

### Dynamic Content
{new Date().getFullYear()} - This will show the current year.

### Conditional Rendering
{process.env.NODE_ENV === 'development' ? (
  <div>Development Mode</div>
) : (
  <div>Production Mode</div>
)}


import { Callout } from 'nextra-theme-docs'

<Callout emoji="👾">
  **Space Invaders** is a 1978 shoot 'em up arcade game developed by Tomohiro
  Nishikado.
</Callout>


import { Tabs, Tab } from 'nextra-theme-docs'
 
<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tab>**pnpm**: Fast, disk space efficient package manager.</Tab>
  <Tab>**npm** is a package manager for the JavaScript programming language.</Tab>
  <Tab>**Yarn** is a software packaging system.</Tab>
</Tabs>